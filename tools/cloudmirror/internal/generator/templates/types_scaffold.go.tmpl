// Code generated by CloudMirror. DO NOT EDIT.
// Generated: {{.GeneratedAt}}
// Service: {{.ServiceName}}
// Protocol: {{.Protocol}}

package {{.PackageName}}

{{if or (eq .Protocol "query") (eq .Protocol "ec2")}}import (
	"encoding/xml"
)
{{end}}
// ============================================================================
// Result Types
// ============================================================================
//
// Result types wrap Query protocol responses and should have XMLName tags.
// The response builder will wrap these in <ActionResponse> and <ResponseMetadata>.
//
// Entity types for AWS resources (Vpc, Instance, Role, etc.) are generated
// in smithy_types.go from AWS Smithy models with correct XML tags.
{{range .Operations}}
// {{.Name}}Result wraps the response for {{.Name}}
type {{.Name}}Result struct {
{{if or (eq $.Protocol "query") (eq $.Protocol "ec2")}}	XMLName xml.Name `xml:"{{.Name}}Result"`
{{end}}	// TODO: Add response fields referencing types from smithy_types.go
}
{{end}}
// EmptyResult for operations that return no data
type EmptyResult struct {
{{if or (eq .Protocol "query") (eq .Protocol "ec2")}}	XMLName xml.Name `xml:""`
{{end}}}

// ============================================================================
// Internal Storage Types
// ============================================================================
//
// Types used for state management (not XML serialization).
// These are for internal emulator state, not AWS API responses.
//
// Example:
//
// type ResourceAttachments struct {
//     AttachedIds []string
// }
